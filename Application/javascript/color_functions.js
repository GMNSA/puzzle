.pragma library
function updateColor() {
    window.color = Qt.rgba(Math.random(),
                           Math.random(),
                           Math.random(),
                           Math.random());
}
